@{
  Layout = "_Layout";
}

@using Treats.Models;

<h1>Welcome to the Pierre's Sweet and Savory Treats</h1>

<hr>

  <h4>Treats</h4>
  @if (Model["treats"].Length == 0)
  {
    <p>No treats have been added yet</p>
  } 
  <ul>
    @foreach (Treat treat in Model["treats"])
    {
      <li>@Html.ActionLink(@treat.Name, "Details", "Treats", new { id = @treat.TreatId})</li>
    }
  </ul>

  <h4>Flavors</h4>
  @if (Model["flavors"].Length == 0)
  {
    <p>No flavors have been added yet</p>
  } 
  <ul>
    @foreach (Flavor flavor in Model["flavors"])
    {
      <li>@Html.ActionLink(@flavor.Name, "Details", "Flavors", new { id = @flavor.FlavorId})</li>
    }
  </ul>

<hr>


@if (User.Identity.IsAuthenticated)
{
  <p>@Html.ActionLink("Manage treats", "Index", "Treats") | @Html.ActionLink("Manage flavors", "Index", "Flavors")</p>
  <p>@Html.ActionLink("Manage orders", "Index", "Orders")</p>
}
else 
{
    <p>Please @Html.ActionLink("log in", "LogIn", "Account") to manage treats and flavors.</p>
}

<p>@Html.ActionLink("Create or manage an account", "Index", "Account")</p>